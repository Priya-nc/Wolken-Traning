package com.wolken.market.map;
//import com.wolken.market.map.Encapsulated;
public interface Abstract {

	boolean save(Encapsulated encap);
	boolean displayAll(Encapsulated encap);
	boolean update(Encapsulated encap);
	boolean deleteByName(Encapsulated encap);
	
}
